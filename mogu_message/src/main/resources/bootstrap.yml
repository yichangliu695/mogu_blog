server:
  port: 8604

spring:
  application:
    name: mogu-message
  cloud:
    nacos:
      username: nacos
      password: nacos

      discovery:
        server-addr: 127.0.0.1:8848
        namespace: dev
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        #指定分组
        group: dev
        #指定命名空间
        namespace: dev